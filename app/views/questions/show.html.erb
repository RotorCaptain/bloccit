<h1>Questions: <%= @question.title %></h1>
 
 <div class="row">
   <div class="col-md-8">
     <p><%= @question.body %></p>
   </div>
   <h4>Resolved: <%= @question.resolved %></h4>

     <%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>
     
   </div>
 </div>
